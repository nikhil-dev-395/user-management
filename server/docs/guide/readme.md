# Functional Requirements

- User Registration with name, email, password, and confirm password. Password must be stored
  securely using password hashing. Proper validations must be applied.
- User Login using email and password. On successful login, session should be created and user redirected to dashboard.
